/**
 * Real Stitch "Recherche (Overlay)" screen's suggestion chips — "Robes, Costumes,
 * Accessoires, Atelier", not the "Robes de mariée, Costumes, Collections, Journal" this
 * page's original plan assumed before checking the real screen (see "Points d'attention").
 * Plain query strings that prefill the search input, not real category slugs.
 */
export const SEARCH_SUGGESTION_CHIPS = ['Robes', 'Costumes', 'Accessoires', 'Atelier'] as const;
