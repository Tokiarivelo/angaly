/**
 * `Atelier` has no `isFlagship`/`isMain` field (not requested by the spec) — the flagship
 * atelier is designated by slug per docs/pages/nos-ateliers-liste.md "Points d'attention".
 * Revisit if a real configurable-highlight need appears.
 */
export const FLAGSHIP_ATELIER_SLUG = 'antananarivo-centre';
