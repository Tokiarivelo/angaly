// Auth layout (connexion/inscription/mot-de-passe-oublié) — Phase 1 adds the
// split-screen photo panel here (docs/pages/authentification.md).
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
