// Authenticated client space (dashboard, appointments, orders, favorites,
// Pattern Studio) — Phase 2 adds the auth guard + sidebar here
// (docs/pages/espace-client-dashboard.md).
export default function ClientLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
