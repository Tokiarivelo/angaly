// Angaly Pattern Studio — distinct dark/champagne identity (palette doc §13).
// Phase 4 adds the Pattern Studio header/stepper shell here
// (docs/pages/pattern-studio-landing.md).
export default function PatternStudioLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
