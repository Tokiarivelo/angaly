// Public marketing/boutique layout — Phase 1 adds the shared Navbar/Footer
// feature components here (docs/pages/home.md, docs/pages/navigation-mobile.md).
export default function PublicLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
