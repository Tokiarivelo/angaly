# Module `orders` — not implemented yet

Scaffolded directory only (Clean Architecture layout: `domain/`, `application/`,
`infrastructure/`, `presentation/`, `__tests__/`). Business logic lands in the
phase that covers this module — see `docs/features/orders.md` for its scope,
endpoints, and Prisma models, and `docs/checklist-implementation.md` for status.

Do not add code here outside of a phase session — see `.cursor/rules/006-phase-workflow.mdc`.
