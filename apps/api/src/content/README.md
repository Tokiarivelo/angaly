# Module `content` — not implemented yet

Scaffolded directory only (Clean Architecture layout: `domain/`, `application/`,
`infrastructure/`, `presentation/`, `__tests__/`). Backs the admin "Gestion de
contenu" page (`PageSection`/`PageSectionVersion` Prisma models) — see
`docs/features/content.md` for scope, endpoints, and Prisma models, and
`docs/checklist-implementation.md` for status.

Do not add code here outside of a phase session — see `.cursor/rules/006-phase-workflow.mdc`.
